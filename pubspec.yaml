name: tehnotop
description: Мобільний додаток ТМ "Технотоп".

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  bottom_navy_bar: ^6.0.0
  flutter_slidable: ^0.6.0
  fluttertoast: ^7.1.8
  shared_preferences: ^2.0.8
  http: ^0.13.4
  intl: ^0.17.0 # форматирование дат
  flutter_cache_manager: ^3.1.3 # управление кешем приложения
  url_launcher: ^6.0.13 # открытие сторонних программ. Например, ссылку на карты Google
  barcode_widget: ^2.0.1 # генерация штрихкода

  cached_network_image: ^3.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

module:
  androidX: true

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/icons/
    - assets/food/
    - assets/offer_banner/
    - assets/restaurants_logo/
    - assets/users/
    - assets/paymentMethod/
    - assets/address_type/
    - assets/notification/

  fonts:
    - family: ACDisplay
      fonts:
        - asset: fonts/ACDisplay-Bold.otf
          weight: 700
        - asset: fonts/ACDisplay-Bold.otf
          weight: 600
        - asset: fonts/ACDisplay-Regular.otf
          weight: 500
        - asset: fonts/ACDisplay-Regular.otf
        - asset: fonts/ACDisplay-Regular.otf
          weight: 300

